// Minimal stub for images reorder route so TypeScript/Next build recognizes this as a module.
// Implement full logic on the host if needed; this prevents build-time errors.
export async function POST(request: Request) {
	
	// Accept request and return 204 No Content as a placeholder.
	return new Response(null, { status: 204 })
}
